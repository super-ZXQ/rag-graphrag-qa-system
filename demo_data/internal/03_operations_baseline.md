# Atlas 当前检索基线（虚构）

## 当前方案

基线采用 800 字符分块、100 字符重叠和单路向量 Top-5 检索。离线抽样显示，缩写、产品编号和错误码问题经常无法召回；长文档中的概括性问题也容易只命中局部段落。

## 评测基线

内部 60 道问题的人工标注结果：Recall@5 为 0.68。回答引用存在率为 0.91，但引用能够直接支持结论的比例为 0.73。

## 运维观察

全量重建索引需要约 4 小时，会影响每日更新。团队希望新方案支持幂等增量索引，并能在模型或向量维度变化时保留旧版本以便回滚。
